# Template business logic will go here
# Example:
# from app.templates.models import Template
# from sqlalchemy.orm import Session
#
# def create_template(db: Session, name: str, content: str):
#     template = Template(name=name, content=content)
#     db.add(template)
#     db.commit()
#     db.refresh(template)
#     return template
